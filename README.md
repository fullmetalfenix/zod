# zod
Trying out Zod for future schema validation in Agent tools.

Plan - Form normalizer - I am going to purposefully have a form submit in different data types than an api uses and standardize them.

note to self - Im using codespaces for this

## React form

This workspace now includes a simple React form with fields for full name, age, date, and a true-or-false radio choice.

Run it with:

```bash
npm install
npm run dev
```

